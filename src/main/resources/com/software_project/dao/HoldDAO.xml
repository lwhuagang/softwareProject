<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.software_project.dao.HoldDAO">
    <update id="updateHoldHH" >
        update soft.hold set hold = #{hold}, holdProfit=#{holdProfit} where userEmail = #{email} and fundCode = #{fundCode}
    </update>
    <select id="getHoldByUserEmailAndFundCode" resultType="com.software_project.pojo.Hold">
        select * from soft.hold where userEmail = #{email} and fundCode = #{fundCode}
    </select>
</mapper>